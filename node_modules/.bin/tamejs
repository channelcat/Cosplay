#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../tamejs/lib/main.js" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../tamejs/lib/main.js" "$@"
  ret=$?
fi
exit $ret
